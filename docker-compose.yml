version: '3'
services:
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - '3000:3000'
  #   environment:
  #     - NODE_ENV=production
  #   depends_on:
  #     - mongodb
  #     - rabbitmq
  mongodb:
    image: mongo:4.4
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db
  rabbitmq:
    image: rabbitmq:3.7
    ports:
      - '5672:5672'
volumes:
  mongodb_data:
